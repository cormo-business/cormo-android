<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 12 이하 호환 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!--FCM 알림 설정-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Neulbeot">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- 회원가입 단계 액티비티들 -->
        <activity android:name=".page.signup_pages.Step01MethodActivity" />
        <activity android:name=".page.signup_pages.Step02TermsActivity" />
        <activity android:name=".page.signup_pages.Step03BirthActivity" />
        <activity android:name=".page.signup_pages.Step04GenderActivity" />
        <activity android:name=".page.signup_pages.Step05PhoneActivity" />
        <activity android:name=".page.signup_pages.Step06PhoneVerifyActivity" />
        <activity android:name=".page.signup_pages.Step07PasswordActivity" />
        <activity android:name=".page.signup_pages.Step08HealthActivity" />
        <activity android:name=".page.signup_pages.Step09ExerciseActivity" />
        <activity android:name=".page.signup_pages.Step10JobActivity" />
        <activity android:name=".page.signup_pages.Step11SedentaryActivity" />
        <activity android:name=".page.signup_pages.Step12NicknameActivity" />
        <activity android:name=".page.signup_pages.Step13ProfilePhotoActivity" />

        <!-- 홈화면 -->
        <activity android:name=".page.home.HomeActivity" />
        <activity android:name=".page.attendance.AttendanceCheckActivity" />
        <activity android:name=".page.exercise.WeekChallengeActivity" />
        <activity android:name=".page.exercise.WithFriendsActivity" />

        <!-- 로그인화면 -->
        <activity android:name=".page.login.LoginMethodActivity"/>
        <activity android:name=".page.login.LoginPhoneActivity" />
        <activity android:name=".page.login.LoginPasswordActivity" />
        <activity android:name=".page.login.LoginWelcomeActivity" />

        <!-- Firebase Messaging Service 등록 -->
        <service
            android:name=".fcm.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM 초기화 자동 등록용 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default" />

    </application>

</manifest>